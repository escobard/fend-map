import React, { Component } from 'react';

import styles from './styles.scss'

export default class Map extends Component {
  
  render() {
    return (
		<div id="map" className="">
			This is where the map will reside
		</div>
    );
  }
}
